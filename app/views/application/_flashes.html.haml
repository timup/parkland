- if flash.any?
  .container
    .row
      .col-sm-12
        - flash.each do |key, value|
          .alert-fixed.alert.text-center.fade.in{class: "alert-#{key}", role: "alert"}
            = value
            %button.close{data: {dismiss: "alert"}}
              %i.fa.fa-times
